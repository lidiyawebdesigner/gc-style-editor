# ── Тяжёлое исходное сырьё (не часть продукта) ──────────────
# Tilda-экспорты и выгрузки уроков курса (~5 ГБ). В репозиторий не нужны.
brief/

# ── Приватное ───────────────────────────────────────────────
# Личные заметки тестирования и фото — не для публичного репозитория.
feedback/

# ── Зависимости ─────────────────────────────────────────────
node_modules/

# ── Локальные бэкапы и временное ────────────────────────────
*.bak
*.bak-*
*.backup
*.backup.*
*.mojibake
*.tmp
**/_extracted/
**/_lesson-text.txt

# ── Локальный конфиг Claude Code (пути конкретной машины) ────
.claude/settings.local.json

# ── Мусор ОС ────────────────────────────────────────────────
.DS_Store
Thumbs.db
